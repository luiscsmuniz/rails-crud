<p id="notice"><%= notice %></p>

<p>
  <strong><%= MiningType.human_attribute_name(:description)%>: </strong>
  <%= @mining_type.description %>
</p>

<p>
  <strong><%= MiningType.human_attribute_name(:acronym)%>: </strong>
  <%= @mining_type.acronym %>
</p>

<%= link_to t('links.edit'), edit_mining_type_path(@mining_type), class: 'btn btn-warning' %>
<%= link_to t('links.delete'), @mining_type, method: :delete, data: { confirm: t('links.confirm') }, class: 'btn btn-danger' %>

<%= link_to t('links.back'), mining_types_path, class: 'btn btn-primary' %>
