<div class="col-md-6 offset-md-3">
	<h1><%=t('links.edit')%></h1>

	<%= render 'form', coin: @coin %>
<br>
	<%= link_to t('links.show'), @coin, class: 'btn btn-primary' %> 
	<%= link_to t('links.delete'), @coin, method: :delete, data: { confirm: t('links.confirm') }, class: 'btn btn-danger' %>
	<%= link_to t('links.back'), coins_path, class: 'btn btn-primary' %>
</div>
